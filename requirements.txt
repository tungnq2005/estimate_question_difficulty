# Core dependencies — ontology build + MCQ difficulty pipeline (Block A + graph features).
# Install:  pip install -r requirements.txt   (or:  pip install -e .)
rdflib>=7.0
networkx>=3.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
owlready2>=0.45
